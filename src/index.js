import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(<App />, document.getElementById('potato'));

// component 는 HTML을 반환하는 함수. 하나의 어플리케이션에 하나의 component 만 렌더링 할 수 있다. component 는 대문자로 시작해야한다 
// javascript 와 HTML의 조합을 jsx 라고 부른다.
